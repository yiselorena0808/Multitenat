import validatorJs from 'validator';
import { SyncValidation } from '../../types.js';
/**
 * Normalize email address
 */
export declare const normalizeEmail: SyncValidation<validatorJs.NormalizeEmailOptions>;
