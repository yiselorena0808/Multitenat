import { SyncValidation } from '../../types.js';
/**
 * Validation signature for the "alpha" regex. Non-string values are
 * ignored.
 */
export declare const alpha: SyncValidation<{
    pattern: string;
}>;
